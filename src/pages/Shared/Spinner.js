import React from 'react';
import './Spinner.css'; // Zaimportuj plik CSS Spinner

const Spinner = () => {
    return (
        <div className="spinner">
            <div className="spinner-inner"></div>
        </div>
    );
};

export default Spinner;
